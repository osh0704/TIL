1xx(정보) : 응답을 받았으며, 처리중에 있다.
2xx(성공) : 요청과 응답이 성공적으로 이루어졌다.
3xx(리다이렉션) : 응답하기 위해 추가적인 조치가 필요하다.
4xx(클라이언트 오류) : 클라이언트쪽의 요청을 처리할 수 없어 오류 발생했다.
5xx(서버 오류) : 서버쪽에서 받은 요청을 처리하지 못하여 오류가 발생헸다.

쉽게 말하자면
100번대와 300번대는 보기 힘들다.
200번대는 서버와 클라이언트 둘 다 잘했다.
400번대는 클라이언트(프론트엔드)가 잘못했다.
500번대는 서버(백엔드)가 잘못했다.

200 OK 
요청과 반환이 성공적으로 이루어졌다. 
일반적으로 GET 요청 후 따라온다.

201 Created 
요청이 성공적이었으며 그로 인해 새로운 리소스가 생성되었다. 
일반적으로 POST 요청 또는 일부 PUT 요청 이후에 따라온다.

400 Bad Request 
클라이언트의 요청 구문이 잘못됬다.
EX) 로그인에서 서버는 아이디를 'ID'라는 이름으로 받고있는데 클라이언트에서 'userID'로 요청해서 에러 발생

401 Unauthorized
요청 처리를 위해 HTTP 인증 정보가 필요함을 알려준다. 접근 허용을 차단하고, 
최초 요청에는 인증 다이얼로그 표시, 두번째는 인증 실패 응답을 보넨디.
EX) 책을 출판하려 하는데 로그인을 하지 않아 저자가 남을 수 없어 출판 실패 에러 발생 

403 Unauthorized
접근 금지 응답. Directory Listing요청 및 관리자 페이지 등을 차단하는 경우의 응답.
허용되지 않은 정보에 접근하려 했을때 발생하는 에러라고 보면된다.
EX) 학교 홈페이지에서 학생이 다른 학생의 비밀글을 보려함 -> 권한 없어서 에러 발생

404 Not Found
클라이언트가 요청한 리소스가 서버에 없다.
EX) naver.com이 아니라 navor.com / naverw.com 과 같이 URL 주소가 잘못돼 주로 발생

500 Internal Server Error 
서버에서 클라이언트 요청을 처리 중에 에러가 발생함.

503 Service Unavallable 
서버가 일시적으로 요청을 처리할 수 없다

504 Gateway Timeout 
서버를 통하는 게이트웨이에 문제가 발생하여 시간이 초과되었다.
요청이 너무 많아 발생한 과부화라고 생각하면 된다. EX) 디도스 맞음

참고 자료
https://codediver.tistory.com/132
